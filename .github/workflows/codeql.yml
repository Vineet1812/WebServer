# For most projects, this workflow file will not need changing; you simply need
# to commit it to your repository.
#
# You may wish to alter this file to override the set of languages analyzed,
# or to provide custom queries or build logic.
#
# ******** NOTE ********
# We have attempted to detect the languages in your repository. Please check
# the `language` matrix defined below to confirm you have the correct set of
# supported CodeQL languages.
#
name: "CodeQL Security Scan"

on:
  push:
    branches: [ "Security_branch" ]
  pull_request:
    branches: [ "Security_branch" ]

jobs:
  analyze:
    name: Analyze
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write

    strategy:
      fail-fast: false
      matrix:
        language: [ 'java', 'javascript' ]  # Add JavaScript to cover HTML/JS content

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3
      with:
        fetch-depth: 0  # Full git history for better analysis

    - name: Setup Java
      uses: actions/setup-java@v3
      with:
        distribution: 'temurin'
        java-version: '11'

    # Initialize CodeQL without requiring an external config file
    - name: Initialize CodeQL
      uses: github/codeql-action/init@v3
      with:
        languages: ${{ matrix.language }}
        queries: security-extended,security-and-quality  # Use extended security queries
        # Inline config instead of using external file
        config: |
          paths:
            - "**/*.java"
            - "**/*.html"
            - "**/*.js"

          paths-ignore:
            - "**/test/**"
            - "**/*.md"
            - ".github/workflows/**"
          disable-default-queries: false

    # Add custom build steps for Java if needed
    - name: Build Java (if needed)
      if: matrix.language == 'java'
      run: |
        # Find all Java files and compile them
        mkdir -p target/classes
        java_files=$(find . -name "*.java" | grep -v "/test/")
        if [ -n "$java_files" ]; then
          echo "$java_files" > sources.txt
          javac -d target/classes @sources.txt || echo "Compilation had issues, continuing with scan"
        else
          echo "No Java files found to compile"
        fi

    # Perform the CodeQL Analysis
    - name: Perform CodeQL Analysis
      uses: github/codeql-action/analyze@v3
      with:
        category: "/language:${{ matrix.language }}"
        upload: true  # Ensure results are uploaded to GitHub Security
